{
  "name": "PRP Cadastro Moradores",
  "install": "sudo apt-get update && sudo apt-get install -y python3-venv && python3 -m venv .venv && ./.venv/bin/pip install --upgrade pip && ./.venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "flask",
      "command": "test -f .env || printf 'SECRET_KEY=%s\\n' \"$(./.venv/bin/python -c 'import secrets;print(secrets.token_hex(32))')\" > .env; ./.venv/bin/python run.py"
    }
  ],
  "ports": [
    {
      "name": "Flask app",
      "port": 5000
    }
  ]
}
